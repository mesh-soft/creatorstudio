{
  "private": true,
  "workspaces": [
    "packages/*",
    "studio"
  ],
  "scripts": {
    "build:tina-packages": "pnpm --dir packages/tinacms run build",
    "dev": "pnpm --dir studio run dev"
  },
  "pnpm": {
    "peerDependencyRules": {
      "allowedVersions": {
        "react": "19",
        "react-dom": "19"
      }
    }
  }
}
